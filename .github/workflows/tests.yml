name: tests-pipelinedp

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  linting:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9"]

    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        # pin on 1.8.5 since it is the latest that supports python 3.8. the lock file changes between 1.8.5 and 2.x
        run: |
          pip install --upgrade pip
          pip install poetry@1.8.5
          poetry install --no-interaction --no-root

      - name: Check formatting
        run: |
          poetry run yapf --diff --recursive pipeline_dp tests analysis

  test:
    runs-on: ${{ matrix.os }}
    needs: [linting]
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.8", "3.9", "3.10", "3.11"]

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: true
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        # pin on 1.8.5 since it is the latest that supports python 3.8. the lock file changes between 1.8.5 and 2.x
        run: |
          pip install --upgrade pip
          pip install poetry@1.8.5
          poetry install --no-interaction --no-root

      - name: Run pytest
        run: |
          poetry run pytest tests analysis/tests
